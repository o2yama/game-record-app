users:
  userId: String #uid
  email: String
  name: String #Default = unknown
  imageUrl: String?

  tokens: #documentのIDとToken統一
    token: String

  games:
    gameId: String
    isFavorite: bool
    isMatch: bool

games:
  gameId: String
  gameTitle: String
  heldAt: Timestamp
  editorKey: String #6文字以上
  readerKey: String #6文字以上
  isMatch: bool
  editorIds: [String]  #editorのuserIdリスト
  readerIds: [String] #readerのuserIdリスト

  teams:
    teamId: String
    teamName: String
    isTeam: bool
    teamTotal: num? #チームだったら

    players:
      playerId: String
      name: String

    fx:
      fxId: String #playerIdと一緒
      d: num
      e: num
      nd: num
      total: num
    ph:
    sr:
    vt:
    pb:
    hb:



